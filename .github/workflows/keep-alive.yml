name: Keep Render Awake

on:
  schedule:
    - cron: '* * * * *'  # every minute UTC

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Render
        run: |
          status=$(curl -o /dev/null -s -w "%{http_code}" https://prathmesh00007-prabhodyanyaya-incident.onrender.com)
          if [ "$status" -ne 200 ]; then
            echo "❌ Ping failed: HTTP $status" >&2
            exit 1
          fi